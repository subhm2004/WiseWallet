# Frontend - Next.js UI
# Copy: cp .env.example .env

# API Gateway URL (used for /api/* rewrites)
API_GATEWAY_URL=http://localhost:8080

# App URL (optional, for metadata/redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000
